[project]
name = "ftp-server"
version = "0.1.0"
description = "FTP Server built with Flet"
requires = ["flet", "pyftpdlib"]

[tool.flet]
# Packages that only have source distributions (for mobile/web builds)
source_packages = ["pyftpdlib"]
